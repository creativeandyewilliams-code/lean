Perform a fresh carrier-complete global-coherence assessment of the Version-23 Studia Logica submission. The expected result GC_sub is an untrusted hypothesis. Execute the frozen review instance rather than predicting its result.

First run the mechanical preflight, qpdf checks, and Lean build/audit. Repair any failing frozen input, synchronize every affected manuscript and map, rebuild, rerun qpdf, and execute `refreeze_review_instance_v23.py` before the semantic trace. Then assess target adequacy, formal correctness, theorem-level novelty, significance, scope, theorem centrality, self-containment, legibility, artifact fidelity, and every recommendation-changing conventional referee objection. Import each recommendation-changing objection as a typed carrier challenge. For every frozen or reopened node, represent all seven actions, compare their projected fitness for the fixed overall submission target, execute only the Choice-selected action, and update history, residue, conditions, and budgets.

Produce a complete independent trace, substantive-warrant report, second-order audit, and one carrier root certificate. The integrated recommendation is part of the root: major revision or reject cannot coexist with GC_sub. If the method is not executed, return NE. If a valid bounded traversal stops with root-relevant residue, return UB. Apply the actual result to the status files and write the external status and audit addendum only after the trace passes the structural validator; do not mutate the frozen PDFs.
