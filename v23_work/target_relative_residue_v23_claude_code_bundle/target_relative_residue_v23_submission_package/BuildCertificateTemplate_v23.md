# Lean Build Certificate - Version 23 (complete after actual build)

- Package version:
- UTC timestamp:
- OS / architecture:
- Lean version (`lean --version`):
- Lake version (`lake --version`):
- Toolchain file SHA-256:
- Lean source SHA-256:
- Exact command: `lake build`
- Exit code:
- Complete stdout/stderr transcript file and SHA-256:
- `grep -R "sorry\|admit"` result:
- `#print axioms` audit result for every exported theorem:
- Warnings:
- Theorem declarations successfully built:
- Claims not encoded:
- Reviewer identity / environment:

A clean build certifies only the listed declarations. It does not certify completion-space semantics, novelty, significance, carrier adequacy, or the independent GCA trace unless those are separately encoded.
