# Executive summary — v17

**Validated:** all 6 executed experiments reproduce bit-for-bit (fingerprints
match; verifier passes).

**Executed (new):** the regenerative receiver study — real isolated same-model
agents, 6 conditions + a second generation. Fatal-error rate 0.000 across all
conditions (incl. the corrupted-artifact trap); the direct-graph+Lean condition
uniquely recovered the registered claim identities (0.786 vs 0.000); Receiver B
regenerated correctly from a transmission alone with 0 author repairs. The
external-trace pipeline was validated on fixtures.

**Open (honest):** the Lean kernel build is blocked by the environment's egress
policy (GitHub release binaries → HTTP 403); the fidelity/CST/historical/physical
agent runs could not complete because the session hit its agent/API limit
mid-run; genuinely external traces, primary physical evidence, and cross-model /
human receiver families remain external inputs. All are re-runnable from the
included code and packets.
