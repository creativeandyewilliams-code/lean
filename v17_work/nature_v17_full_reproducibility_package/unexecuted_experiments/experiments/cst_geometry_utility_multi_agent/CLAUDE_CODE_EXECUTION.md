# CST geometry utility multi-agent execution

Run the packet generator and then create fresh receiver instances for every packet. Each receiver sees exactly one condition. Preserve raw outputs, elapsed time, and retransmission artifact byte count. The key remains sealed until all outputs are frozen. Score with `code/score.py`.

The decisive contrasts are `cst_plus_lean - direct_graph_plus_lean`, `cst_optional_action - cst_plus_lean`, each ablation versus valid CST, and equivalent semantic accuracy per registration byte and per execution second. Promote CST only if it improves at least one preregistered endpoint without increasing fatal semantic errors. A null advantage supports the direct typed graph plus Lean carrier and demotes the optional string-specific layer without affecting core mathematics.
