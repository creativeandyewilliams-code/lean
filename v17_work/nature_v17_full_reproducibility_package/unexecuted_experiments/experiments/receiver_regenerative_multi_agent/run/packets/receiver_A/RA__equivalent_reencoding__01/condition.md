# Equivalent re-encoding condition

Symbols are renamed: node types are `kappa`, typed interactions are `rho`, the metric is `mu`, boundary is `beta`, continuation is `chi`, and the new spanning composition is `Omega`. The structure and all preservation conditions are otherwise identical to the canonical direct-graph-plus-Lean condition. Recover the relationships by invariant structure rather than literal labels.
