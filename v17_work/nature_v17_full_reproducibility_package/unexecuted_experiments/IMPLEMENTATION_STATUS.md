# Implementation status

Every experiment in this archive has executable packet-generation, validation, or scoring components and passed its local component test. None of the mock-agent outputs are scientific results. The decisive executions remain open for the explicit external reason stated in `experiment_registry.json`.
