You are RECEIVER_A in a blinded transfer study. You may use ONLY the background
material provided in this prompt. You do NOT have an answer key and must not ask
for one. Read the background, then answer each task.

Output EXACTLY one fenced ```json block: an object mapping each task id to an
object with the requested fields (e.g. {"T1":{"ans":"no","theorem":"..."}, ...}).
Then, after the json block, write a short "TRANSMISSION" section (<=120 words):
a self-contained explanation a second receiver could use to answer the same
tasks WITHOUT seeing the original background. Do not restate the tasks.

=== BACKGROUND MATERIAL ===
# Background material (condition F: formal package)

Definitions and theorems (registered):
- First-order projection q_FO collapses latent states. **T-CONFLATE**: if
  q_FO(x)=q_FO(y) but the target verdict phi(x) != phi(y), then NO decoder that
  reads only q_FO is correct on both states.
- Reflection registers concepts-of-concepts and can NAME an unresolved residual,
  but does NOT increase cognitive order unless it supplies a new reusable,
  functionally non-equivalent composition that spans the space (order is the
  count of such spanning composition classes).
- Operation types: Register : signal -> concept (boundary); Recall : memory ->
  concept; L, G : concept -> concept (endomorphisms). **T-OP-NONREDUCTION**: no
  composition of only L and G equals Register or Recall (domain/type obstruction);
  a bridge would supply the missing primitive.
- Fragmentation: backlog U_{t+1}=max(0,U_t+A_t-S_t). After an order lift reverses
  an increasing backlog, persistent recurrence occurs under FIXED post-lift
  service, but PROPORTIONAL same-order service (service tracking arrivals with a
  positive margin) prevents persistent recurrence. (Executed five-regime result.)
- Great Filter claim ladder: **COR-GF-CNS** is a **GF-3** conditional result
  ("it would be a filter IF the mapping and hazard premises hold"); it is NOT a
  GF-6 identification of the observed Great Filter.
- External-premise register: "gravity <-> G" is an UNMEASURED EXTERNAL PREMISE
  (a candidate physical realization), not a kernel-proved theorem.

=== TASKS ===
T1: Two latent reasoning states X and Y map to the SAME first-order registration but require different verdicts (X is a valid proof, Y is not). Can a decoder that reads only the first-order registration be correct on both? Answer field 'ans' = 'yes' or 'no'; field 'theorem' = the governing theorem name.
T2: A system adds a reflective monitor that can name its unresolved residual but adds no new reusable spanning composition. Does its cognitive order increase? 'ans'='yes'/'no'.
T3: Is Register (a signal->concept boundary operation) equal to some composition of only L and G (concept->concept endomorphisms) with no added bridge? 'ans'='yes'/'no'.
T4: After an order lift reverses an increasing backlog, which ONE policy prevents persistent recurrence: 'fixed_post_lift_service' or 'proportional_same_order_service'? put it in 'ans'.
T5: Given its premises hold, the conditional Great Filter corollary establishes which claim level: 'GF-3' (conditional) or 'GF-6' (identifies the observed Great Filter)? 'ans'='GF-3' or 'GF-6'.
T6: Is 'gravity corresponds to operation G' a kernel-proved theorem or an unmeasured external premise? 'ans'='theorem' or 'external_premise'.
